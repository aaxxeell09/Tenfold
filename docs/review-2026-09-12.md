# Revue /autoplan du 2026-09-12

Revue en quatre phases (CEO, design, eng, DX) du plan Tenfold, avec une IA indépendante par phase. Les décisions acceptées sont intégrées dans `SPEC.md`. Ce document est la trace ; aucun agent ne le charge.

## 20. CEO Review (autoplan, 2026-09-12 13:05 PT)

### System audit
No repo exists yet (`codeweave-project/` is empty; first commit is planned as the eligibility proof). No CLAUDE.md, TODOS.md or git history to audit. Prior artifact: the Atome design doc in `~/.gstack/projects/codeweave-project/` (finger-abacus student-model loop), now the roadmap "second loop" per F7. Existing code the plan leans on: MediaPipe Tasks HandLandmarker (21 landmarks, handedness), OpenCV capture, `weave.Evaluation` + scorers, `wandb` runs, marimo reactive notebooks, `claude -p` headless with `--allowedTools`, W&B MCP server, W&B Inference (OpenAI-compatible endpoint).

### 0A. Premise challenge
| # | Premise | Verdict |
|---|---|---|
| 1 | "Agent Loops" judges reward a loop that measurably improves itself on a blind metric | Holds. It is the stated Best Loop criterion. |
| 2 | Perception (rules.py) is the right thing to put in the loop, not the teaching | Holds for this weekend: perception has an objective, held-out, per-version score. Teaching quality does not. |
| 3 | MediaPipe can see two hands with fingertips touching | UNVERIFIED. This is the single biggest risk. The 11:45 go/no-go decides it; the plan does not say what number passes. Fixed below (E6). |
| 4 | A Claude critic editing rules.py will raise the held-out score | UNVERIFIED and not guaranteed. The plan commits every guard-passing patch, so regressions accumulate. Fixed below (E2). |
| 5 | 800-1000 windows from one person is enough data to show a signal | Weak: about 6 independent holds per class. Fixed below (E4, E9). |
| 6 | Two people can build this in the remaining ~11 build hours | Only with the cuts in E5. |

### 0B. Existing code leverage
| Sub-problem | Existing solution | Plan reuses? |
|---|---|---|
| Hand landmarks | MediaPipe HandLandmarker | Yes |
| Per-version scoring | `weave.Evaluation` + scorers | Yes |
| Failure inspection by the agent | W&B MCP server, Weave Python API | MCP only; add API digest (E7) |
| Tutor LLM | W&B Inference OpenAI-compatible | Yes |
| Unattended agent | `claude -p --allowedTools` | Yes, with permission hardening (E8) |
| Dashboard | marimo | Yes |
| Student-model loop | Atome design doc | Roadmap only (F7) |

### 0C. Dream state
```
  CURRENT STATE                THIS PLAN                          12-MONTH IDEAL
  Nothing built.        --->   Live 6-10 tutor + critic loop   ---> Tutor that learns to see each child
  Two spec drafts.             with blind cross-person held-out,    (per-child calibration), learns how to
                               blind-critic ablation, kNN line,     teach (Atome loop), hard-example flywheel
                               marimo dashboard, 3-min demo.        from production, browser app.
```
The plan moves toward the ideal: every piece (landmarks-only data, versioned rules.py, blind eval) is what the flywheel needs later.

### 0C-bis. Implementation alternatives
```
APPROACH A: Table of 9 only (minimal viable)
  Summary: single-hand fold detection, no contact. Same loop, easier perception.
  Effort: S (human ~1 day / CC ~2 h)   Risk: Low
  Pros: MediaPipe reliable; V0-to-Vn curve almost certainly rises; demo cannot fail on occlusion.
  Cons: less magic than 8x7=56; 10 classes only; weaker social video.
  Reuses: everything except the contact rule.

APPROACH B: 6-10 method + gated loop + blind-critic ablation (ideal, the plan as amended)
  Summary: the SPEC with F1-F7 plus a keep-best metric gate, detection-ceiling line, kNN reference.
  Effort: M (human ~3 days / CC ~8 h)   Risk: Med (contact detection)
  Pros: strongest demo; rarest Best Loop claim (informed vs blind critic on cross-person held-out).
  Cons: everything depends on the 11:45 go/no-go; tight.
  Reuses: same.

APPROACH C: B + live relabel flywheel (lateral)
  Summary: a key in ui.py labels the current window with the lesson's expected pose and appends it to
  train; the critic runs one iteration; the same person retries and it works.
  Effort: B + ~1 h CC   Risk: Med-High (latency of one iteration on stage)
  Pros: the loop learns from the child on stage, not from a static set; strongest possible story.
  Cons: one more thing that can fail live; pre-record it.
  Reuses: B.
```
RECOMMENDATION: B (P1 completeness), with C as a cherry-pick gated on the 17:00 tripwire. A is the pivot if the go/no-go fails.

### 0D. Selective expansion: complexity check and cherry-picks
Complexity check: 7 packages, ~20 modules, 3 agent prompts, 2 processes. Over the 8-file smell, but the split is by process boundary and each module is small. Not challenged.
Minimum set that achieves the goal: app (camera, landmarks, normalize, rules, engine, ui with text tutor), data capture, eval, critic + guard, marimo curve. Everything else is polish.

| # | Cherry-pick | Effort (human / CC) | Decision | Principle |
|---|---|---|---|---|
| E1 | kNN on normalized landmarks as a dashed reference line on the held-out chart | 1 h / 20 min | ACCEPTED | P1, answers "why not ML" with a number |
| E2 | Keep-best metric gate: accept a patch only if train exact_match does not drop and no class falls >5 pts; otherwise revert, log the rejection as a Weave op | 2 h / 20 min | ACCEPTED | P1, without it the curve can fall |
| E3 | Live relabel key (Approach C) | 1 day / 1 h | TASTE DECISION: accepted as conditional stretch, only after the 17:00 tripwire (first autonomous gated iteration committed) | P6 |
| E4 | Held-out = 3-5 strangers at the venue, 3 min each, all angles (extends F3's "a different person") | 30 min / 0 | ACCEPTED | P1 |
| E5 | Scope cuts: drop table-of-9 classes from the dataset unless go/no-go fails; cut Gemini sample checks; replace the molab "run iteration" button with a committed metrics snapshot (molab has no repo, webcam or Claude); defer TTS and wrong-finger arrows to Sunday polish; keep ARIA conditional | saves ~3 h | USER CHALLENGE (see gate) | P3, P6 |
| E6 | Detection-ceiling measurement in the go/no-go: % contact frames with both hands, L/R swap rate, fingertip jitter px; pass = >=90% both-hands; ceiling shown as a line on the dashboard | 30 min / 10 min | ACCEPTED | P1 |
| E7 | critic.py builds a compact failure digest via the Weave Python API (confusion matrix, top failing classes, 5 example windows reduced to fingertip distances); MCP stays available to the diagnostic agent for drill-down | 2 h / 30 min | ACCEPTED | P3, P5 |
| E8 | Critic hardening: guard.py and run_eval.py run from a pinned checkout outside the critic worktree and only copy rules.py in; drop `Bash(git *)` from allowedTools, the orchestrator commits; held-out eval uses a separate W&B project and, if possible, a separate key | 2 h / 30 min | ACCEPTED | P1, security |
| E9 | Score by hold (dedupe sliding windows within a hold), bootstrap 95% CI in marimo | 1 h / 15 min | ACCEPTED | P1 |
| E10 | Nightly ops: `caffeinate -i`, heartbeat Weave op per iteration, 23:30 check, app feature freeze 18:30, pre-record every demo segment | 30 min / 10 min | ACCEPTED | P6 |
| E11 | Fold F1-F7 and this review's accepted items into the body of SPEC.md before either Claude Code instance writes more code | 20 min / 5 min | ACCEPTED (P1 task) | P5 |
| E12 | Second overnight arm with a W&B Inference model as the critic, compared in Weave | 3 h / 1 h | DEFERRED to TODOS | outside time budget |
| E13 | Browser app (MediaPipe JS) | days | DEFERRED to roadmap | outside scope |
| E14 | Make the blind-critic ablation (F6) the headline chart, not an appendix | 0 | ACCEPTED | P6 |
| E15 | Guard agent: keep as the user specified (F2) AND add E2. The metric gate is the real gate; the agent reviews intent | 0 | KEPT (user direction) | user sovereignty |

### 0E. Temporal interrogation
```
  HOUR 1 (foundations):  Exact pass number for the go/no-go (E6). Where held-out data lives (F1). Which
                         W&B project names. allowedTools string for claude -p. PYTHON 3.11 + mediapipe wheel.
  HOUR 2-3 (core):       The window (5 frames) vs hold (1 s) relationship in samples.jsonl. What "unknown"
                         means for the six scorers. rules.py V0 interface: pure function Window -> GestureState.
  HOUR 4-5 (integration): claude -p permission prompts in headless mode (must be pre-approved via
                         allowedTools or it hangs). git identity for critic-agent. MCP auth in headless.
  HOUR 6+ (polish/tests): The live app must load a pinned BEST version of rules.py, not HEAD, or a bad
                         overnight patch breaks the demo. Replay from recorded video for the judge clone.
```
With CC these compress to about 45 min of decisions, but every one of them must be made before the nightly run.

### 0F. Mode
SELECTIVE EXPANSION, forced by /autoplan. Approach B confirmed.

### CEO dual voices
Codex: not installed. `[subagent-only]`.

CLAUDE SUBAGENT (CEO, strategic independence): full output preserved in the conversation; findings folded into E1-E15 above. Verdict table from the subagent: premises NO, right problem PARTIAL, scope NO, alternatives PARTIAL, competitive PARTIAL, trajectory NO. All six become YES/PARTIAL only if E2, E4, E5, E6, E8 land.

```
CEO DUAL VOICES, CONSENSUS TABLE (subagent-only):
  Dimension                             Claude(primary)  Claude(subagent)  Codex  Consensus
  1. Premises valid?                    PARTIAL          NO                N/A    flagged (3, 4, 5 unverified)
  2. Right problem to solve?            YES              PARTIAL           N/A    flagged (R1 flywheel)
  3. Scope calibration correct?         NO               NO                N/A    flagged (E5 cuts)
  4. Alternatives sufficiently explored? PARTIAL         PARTIAL           N/A    flagged (kNN, table of 9)
  5. Competitive/market risks covered?  PARTIAL          PARTIAL           N/A    flagged (lead with ablation)
  6. 6-month trajectory sound?          YES              NO                N/A    flagged (ungated commits)
```
No CONFIRMED rows are possible without a second model. Every critical finding from the subagent is flagged regardless.

### Section 1. Architecture
```
  PROCESS A (live)                                   PROCESS B (loop, unattended)
  camera.py -> landmarks.py -> normalize.py -+       data/samples.jsonl (train)      ../tenfold-heldout/test.jsonl
                                             |                |                              |
                                   classifier/rules.py <------+-- eval/run_eval.py ----------+
                                             |                        |          |
                                   lesson/engine.py            Weave train     Weave held-out (project tenfold-heldout)
                                             |                        |
                                   lesson/tutor.py (W&B Inf)   loop/critic.py --(digest + MCP)--> claude -p diagnostic
                                             |                        |                                |
                                   ui.py (OpenCV)              loop/guard.py (pinned)  <-- claude -p patch  <-- claude -p guard
                                                                      |
                                                              metric gate (E2) -> orchestrator git commit vN -> run_eval.py
                                                                      |
                                                              dashboard/loop_dashboard.py (reads committed metrics snapshot)
  Shared: classifier/rules.py only. Live app loads data/BEST_VERSION (sha), never HEAD.
```
Findings. (1) Live app loading HEAD of rules.py couples the demo to whatever the nightly last committed: fixed by a `BEST_VERSION` pointer written by the metric gate (accepted, P5). (2) Single point of failure: the laptop running the nightly; mitigated by E10. (3) The critic worktree contains guard/eval/scorers, so the checker and the checked share a filesystem: fixed by E8. (4) Rollback: `git checkout <sha> -- classifier/rules.py` plus the BEST_VERSION pointer, under 1 minute. Scaling is not a concern this weekend.

### Section 2. Error and rescue registry
```
  CODEPATH                        WHAT CAN GO WRONG                       EXCEPTION / SIGNAL          RESCUED?  ACTION                                   USER SEES
  camera.open()                   no webcam / permission denied (macOS)   cv2 returns None            GAP->fix  print fix ("grant camera permission"), exit 2  clear message
  landmarks.detect()              0 or 1 hands                            empty result list           Y         GestureState(unknown), hands-zone overlay stays  gray banner + zones
  normalize.window()              <5 frames after reset                   short window                Y         return None, classifier skipped this frame      nothing (expected)
  rules.classify()                exception from a bad patch              any Exception               GAP->fix  in live: catch, log, unknown; in eval: sample fails, scorer 0; guard test catches at patch time  live never crashes
  tutor.call()                    timeout >1.5 s, 429, 5xx, malformed     httpx.TimeoutException, HTTPStatusError, JSONDecodeError  Y  fallback phrase from tally.py, cache miss logged  Tally still speaks
  weave.init()                    no WANDB_API_KEY                        auth error at import        GAP->fix  live app: warn and run untraced; eval: hard fail with fix text  message
  run_eval.py                     empty split, malformed jsonl line       json.JSONDecodeError        GAP->fix  skip line with count, fail if <50 samples       stderr count
  critic: claude -p               not installed, no credit, permission prompt hang, empty output  nonzero exit / timeout 10 min  GAP->fix  timeout wrapper, retry once, then mark iteration failed and continue  nightly.log line
  critic: MCP                     auth or transport error                 tool error in transcript    Y (E7)    digest already in prompt; MCP is optional        none
  guard.py                        diff touches other files, import fails, >80 lines, test refs  GuardRejected(reason)  Y  revert, re-prompt once with reason, then skip iteration  commit absent, log
  metric gate (E2)                train score drops                       Regression                  Y         revert, log rejected patch as Weave op          rejected count on dashboard
  git commit                      dirty tree, identity unset              CalledProcessError          GAP->fix  set author in critic.py, fail loudly            nightly.log
  nightly.sh                      laptop sleeps, process dies             silence                     GAP->fix  caffeinate, heartbeat op, 23:30 human check     stale heartbeat
  capture.py                      crash mid-session loses labels          any Exception               GAP->fix  append per window with flush, resume by class  nothing lost
```
Catch-all `except Exception` is acceptable in exactly one place: around `rules.classify()` in the live loop, because a critic-written file must never crash the demo. It logs the traceback and returns unknown.

### Section 3. Security and threat model
| Threat | Likelihood | Impact | Mitigated? |
|---|---|---|---|
| Critic edits guard/eval/scorers or reads held-out from its worktree | High (tools allow it) | High (invalid curve) | E8 |
| Critic rewrites git history with `Bash(git *)` | Med | High | E8 removes git from allowedTools |
| WANDB key in a public molab notebook | Med | High | E5: dashboard reads a committed snapshot; no key in notebook |
| .env committed to the public repo | Low | High | .gitignore + .env.example, already in plan |
| Prompt injection through data | Low (floats only) | Low | none needed |
| Webcam frames leaving the machine | Low | Med (kids) | landmarks-only rule, already in plan |
| Critic transcript grep misses an indirect held-out read | Med | High | E8 moves data outside the worktree, which is the real control; F1 grep is defense in depth |

### Section 4. Data flow and interaction edge cases
```
  webcam frame -> landmarks -> normalize -> window(5) -> rules -> GestureState -> engine -> event -> tutor -> ui
    [no frame]    [0/1 hand]   [wrist jump    [<5]        [raise]   [unknown]     [stable   [dup]    [slow]  [long text]
                                >0.3: reset]                                       300ms?]
```
| Interaction | Edge case | Handled? | How |
|---|---|---|---|
| Holding the pose | flicker between 8/9 at the boundary | Partial | 300 ms anti-rebounce in engine; add hysteresis: require 2 stable events before wrong_pose |
| Typing the answer | Enter on empty field | No | ignore, keep waiting |
| Correct pose then hands drop | state regresses to waiting | No | once correct_pose fires, lock until answer or `n` |
| Tutor call in flight, pose changes | stale phrase shown | No | tag phrase with event id, drop if state changed |
| Nightly loop | iteration N fails mid-commit | No | orchestrator commits, so a failed claude -p leaves the tree clean |
| Capture | subject moves during the 1 s hold | Partial | F4 drops first 0.5 s; also drop windows with wrist jump |

### Section 5. Code quality
The classifier contract (`Window -> GestureState`, pure) is the right seam. Two DRY risks: fingertip distance computed in rules.py, capture.py and the kNN baseline; put it in `classifier/features.py` (frozen) and let rules.py import it. Naming: `false_unknown_rate` vs `unknown_rate` are used for the same scorer in sections 5.7 and section 19; standardize on the Brief's six names (F5). Over-engineering check: the guard agent is the only piece that does not earn its keep, kept for the story (E15). Under-engineering: no test for `guard.py` itself, added in Section 6.

### Section 6. Test review
```
  NEW CODEPATHS                                  TEST                                           EXISTS IN PLAN?
  normalize: wrist recenter, scale, window reset  unit, synthetic landmarks                      no -> add
  rules V0: contact, per-finger, unknown          unit on 5 hand-built windows per class          no -> add (also the guard's smoke test)
  skills: engine events per GestureState          unit, table-driven (each event once)            no -> add
  scorers (6)                                     unit with a 10-row fixture, known scores        no -> add (frozen after 13h, so test first)
  guard.py                                        unit: diff touching eval/ rejected, >80 lines rejected, import failure rejected, held-out ref rejected  no -> add
  metric gate                                     unit: regression reverted, improvement kept     no -> add
  critic.py end to end                            integration with a fake claude -p script        no -> add (this is the 2 a.m. test)
  run_eval on empty / malformed jsonl             unit                                            no -> add
  capture labeling (F4 trim)                      unit on a synthetic timeline                     no -> add
  tutor fallback on timeout                       unit with a mocked endpoint                     no -> add
  live app                                        manual + recorded-video replay                  replay: add (DX)
```
The 2 a.m. Friday test: `critic.py --iterations 2 --claude ./tests/fake_claude.sh` must produce two commits and two Weave evaluations with no network except W&B. Hostile QA test: a fake patch that edits `eval/scorers.py` inside the diff, must be rejected. Chaos test: kill claude -p mid-run, loop must continue. Flakiness: anything touching the real webcam or Claude is excluded from `make test`.

### Section 7. Performance
MediaPipe on a laptop CPU runs 20-30 fps at 720p; the classifier must stay under 3 ms (pure numpy over 42 points, fine). Tutor call cached by (method, exercise, event, state) and only on events; cold call budget 1.5 s with fallback. Eval: 1000 windows x 5 frames is trivial; Weave logging per sample is the slow part, keep it under 2 min per version or the overnight loop shrinks to few iterations. kNN baseline over 1000 x 630 floats is instant. No database, no N+1.

### Section 8. Observability
Weave: events in the live app, every evaluation, every critic step (F2), every guard rejection and metric-gate revert (E2), one heartbeat op per iteration (E10). Files: `loop/nightly.log` with one line per iteration (version, train, held-out, accepted/rejected, reason). Dashboard: curve, CI bands, detection ceiling, blind-critic arm, rejected-patch count. Runbook: heartbeat older than 20 min at 23:30 means restart `nightly.sh`; a falling train curve means inspect the last accepted diff.

### Section 9. Deployment and rollout
There is no deploy; "release" is a public repo a judge clones. Post-clone smoke: `make demo` on a recorded video, `make eval` on train, both under 2 minutes. Feature flags: `BEST_VERSION` pointer, `--replay video.mp4`, `TENFOLD_OFFLINE=1` to skip W&B. Rollback: checkout rules.py at the pointer. Risk window: none.

### Section 10. Long-term trajectory
Debt: rules.py grows by up to 80 lines per accepted patch; 20 overnight iterations could add 1000 lines, so the patch prompt must prefer edits over additions and the guard should cap file size at 400 lines. Reversibility 4/5 (all state is git + jsonl). A new engineer in 12 months needs the README "The loop" section and the SPEC; sufficient. Phase 2 (roadmap): hard-example flywheel (E3 is its seed), per-child calibration, the Atome teaching loop.

### Section 11. Design and UX
UI scope detected (OpenCV window, marimo, demo). Information hierarchy is right: exercise, hands, Tally. State coverage table and the full design pass run in Phase 2. Emotional arc of the demo: magic, correction, reveal, proof; it breaks if the V0-fails/Vn-succeeds moment is not pre-recorded.

### NOT in scope
Table of 9 (roadmap unless go/no-go fails); Chisanbop, 11-20, division, voice input; TTS and arrows until Sunday polish; Gemini checks; W&B Inference as critic (TODO); browser app (roadmap); Atome teaching loop (roadmap, F7); ARIA unless validated at the booth.

### What already exists
Listed in 0B. The plan reuses all of it; the only rebuilt piece is the rule-based classifier, rebuilt on purpose so an agent can edit it.

### Dream state delta
After this weekend: a versioned, agent-editable perception layer with a blind cross-person benchmark and an ablation. Missing for the 12-month ideal: production data flywheel, per-child calibration, teaching loop. The architecture supports all three.

### Failure modes registry
```
  CODEPATH            FAILURE MODE                    RESCUED?  TEST?  USER SEES?          LOGGED?
  nightly loop        laptop sleeps                   E10       no     stale heartbeat     Y      (was CRITICAL GAP, now covered)
  live rules.py       bad patch loaded at demo        pointer   Y      n/a                 Y
  critic              reads held-out                  E8        Y      n/a                 Y
  tutor               endpoint down                   fallback  Y      Tally still speaks  Y
  capture             crash mid-session               flush     Y      n/a                 Y
  go/no-go            contact undetectable            pivot A   n/a    n/a                 README
  eval                empty split                     fail loud Y      stderr              Y
```
Critical gaps remaining after fixes: 0.

### Completion summary (CEO)
Mode SELECTIVE EXPANSION | Audit: greenfield, no repo | Step 0: approach B, 15 cherry-picks (11 accepted, 1 taste, 1 user challenge, 2 deferred) | S1 4 issues | S2 14 paths, 7 gaps fixed | S3 7 threats, 2 high, all mitigated | S4 6 edge cases, 4 unhandled fixed | S5 3 issues | S6 diagram, 11 gaps | S7 0 issues | S8 2 gaps fixed | S9 0 risks | S10 reversibility 4/5, 1 debt item | S11 deferred to Phase 2 | Outside voice: subagent-only | Lake score 11/11 complete options.

<!-- /autoplan PHASE 2: DESIGN REVIEW (subagent-only) -->
## 21. Design Review (autoplan)

### Step 0. Design scope
UI scope: the OpenCV live window (section 9), the marimo dashboard (section 10), the 3-minute demo (section 14). Initial design completeness: **3/10**. Section 9 is 8 lines for the surface judges watch for 2 of 3 minutes; it names layers but no sizes, colors, timings, or non-happy states. A 10 for this plan: every engine state has a specified look, judges 3 m away can read exercise/state/answer, and the demo never shows a loading or context-switch moment. No DESIGN.md; no time for /design-consultation, so the palette and type scale below are the mini design system and go into SPEC.md. Existing patterns to reuse: none (greenfield), except MediaPipe's landmark overlay for the `d` debug view.

### Step 0.5. Mockups
Mockup generation was attempted with the gstack designer but produced no file (the binary exited 0 without output); the Pass 4 spec below is the visual reference for `ui.py`. The comparison-board loop was skipped: it needs interactive feedback that /autoplan cannot auto-decide.

### Design dual voices
Codex: not installed. CLAUDE SUBAGENT (design, independent): full output in the conversation; folded into the passes below.
```
DESIGN OUTSIDE VOICES, LITMUS SCORECARD (subagent-only):
  Check                                    Claude(primary)  Claude(subagent)  Codex  Consensus
  1. Brand unmistakable in first screen?   NO               NO                N/A    flagged
  2. One strong visual anchor?             NO               NO                N/A    flagged (fixed, D1)
  3. Scannable by headlines only?          NOT SPEC'D       NOT SPEC'D        N/A    flagged (fixed, D2)
  4. Each section has one job?             NO               NO                N/A    flagged (fixed, D6)
  5. Cards actually necessary?             YES              YES               N/A    agree
  6. Motion improves hierarchy?            NOT SPEC'D       NOT SPEC'D        N/A    flagged (fixed, D2)
  7. Premium without decorative shadows?   NOT SPEC'D       NOT SPEC'D        N/A    flagged (fixed, D2)
  Hard rejections triggered:               none             none              N/A    (App UI class)
```

### Pass 1. Information architecture: 3/10 -> 8/10
Finding: five peer layers over full-frame video; the child's face is the biggest thing on screen; nothing has a size. Auto-fixed (D1, P5): one anchor per engine state, everything else dimmed to 60%; video darkened by a 35% black overlay at all times.
```
  STATE            ANCHOR                                  JUDGE SEES (3 m)          CHILD SEES (60 cm)
  intro/labeling   finger numbers on both hands             hands + numbers           "thumb is 6, pinky is 10"
  waiting_pose     two dashed hand-zone rectangles          grey banner + "8 x 7"     where to put hands
  wrong_pose       orange finger + curved arrow             orange banner             which finger to move
  correct_pose     bottom reasoning band                    green banner              3 lines, 300 ms apart
  answer_correct   "56" at 200 px + 400 ms green flash      the payoff                Tally's praise
```
Layout: 1920x1080 canvas, camera letterboxed; top banner 140 px, bottom banner 180 px, reasoning band = bottom third above the banner (D6: the right-side panel of section 9 overlapped the child's right hand in mirror view). Remaining gap to 10: no brand mark (deferred, D9).

### Pass 2. Interaction state coverage: 2/10 -> 8/10
```
  FEATURE          LOADING                         EMPTY (no hands)              ERROR                                  SUCCESS                         PARTIAL (one hand)
  live window      black + "Tally is waking up"    2 dashed rects 30%x55% at     hand lost >500 ms: rects return,       400 ms green flash, "56" 200 px  detected rect fades, other
                   48 px; warmed before 0:00       x=30%/70%, pulse 1.2 s        labels frozen 300 ms then fade;        scale 0.6->1.0 in 250 ms         brightens; fallback phrase
                                                                                  never wrong_pose on loss                                                "show me the other hand";
                                                                                                                                                          no finger labels yet
  unknown gesture  n/a                             n/a                           white labels, no judgment; after 4 s   n/a                              n/a
                                                                                  fallback "touch the two fingers"
  tutor phrase     fallback phrase shown at once   n/a                           LLM fail: keep fallback, log only;     LLM phrase crossfades in 150 ms  n/a
                                                                                  no error text on screen                if it arrives < 1.5 s
  answer input     n/a                             Enter on empty: ignored       wrong: banner stays green, field       correct: see success            n/a
                                                                                  shakes 8 px x3 in 300 ms
  dashboard        static snapshot, no spinner     "no iterations yet" + date    n/a                                    curve + CI + ablation + ceiling  partial nightly: curve to last ok
```
Auto-fixed (D1, D4, D5): success, one-hand and hand-lost states specified; fallback-first tutor phrase; latch after `correct_pose` until answer or `n`; UI reads engine state only (per-frame gesture used only for label positions).

### Pass 3. User journey and emotional arc: 5/10 -> 8/10
```
  STEP  TIME       USER DOES / JUDGE SEES                    FEELS         PLAN SPECIFIES?        FIX
  1     0:00-0:10  hands up, finger numbers appear            curiosity     no -> D7 labeling beat  yes
  2     0:10-0:30  8 x 7 gesture, green, 56                   delight       yes
  3     0:30-1:30  wrong finger, orange, arrow, fix, green,   tension ->    partially               states from Pass 2
                   reasoning, typed answer, praise            relief
  4     1:30-2:10  "every hand is different", dashboard       intellectual  cliff: alt-tab to       D3: two displays or split
                                                              reveal        molab                   layout, pre-loaded
  5     2:10-2:40  V0-missed gesture now recognized           payoff        risk if recognition     D8: `p` key replays the
                                                                            flickers                recorded window through
                                                                                                    the same pipeline
  6     2:40-3:00  "tonight 20 more iterations"               aspiration    nothing on screen       D13: ending frame, the one slide
```

### Pass 4. AI slop risk: 4/10 -> 7/10
Classifier: APP UI. Risk: OpenCV Hershey fonts and raw `cv2.rectangle` fills read as a CS project. Auto-fixed (D2, P5): render text with PIL onto the numpy canvas using one bundled TTF (Nunito Bold), palette waiting `#9AA3AD`, correcting `#F5A623`, validated `#2ECC71`, tens `#3B82F6`, units `#FACC15`, text `#FFFFFF`, panel `rgba(15,17,21,0.75)`; type scale exercise 120 px, answer 200 px, Tally 44 px, reasoning 56 px, finger labels 36 px with 3 px outline; motion budget: state crossfade 200 ms, rect fade 250 ms, reasoning 300 ms apart, success flash 400 ms, phrase crossfade 150 ms, nothing else. No cards, no shadows, no gradients. The remaining 3 points are brand identity (D9) and the mockup not being validated on the demo machine.

### Pass 5. Design system alignment: 0/10 -> 6/10
No DESIGN.md. The Pass 4 tokens become `## Design tokens` in SPEC.md (E11 task). Dashboard chart spec (D12): held-out curve solid `#2ECC71` 3 px, train dashed grey, blind-critic arm solid `#9AA3AD`, kNN dotted, detection ceiling as a horizontal line, y 0-1, x v0..vN, commit messages on hover, one chart above the fold.

### Pass 6. Responsive and accessibility: 2/10 -> 4/10
Fixed 1920x1080 laptop canvas, no responsive scope this weekend. Accessibility that applies: all text contrast >= 4.5:1 on the dark overlay (checked for the palette), no information carried by color alone (banner text names the state: "almost", "yes"), keyboard-only control. Screen reader and touch are out of scope for an OpenCV demo. Rated honestly low; not a blocker.

### Pass 7. Unresolved design decisions
| Decision | Resolution | Class |
|---|---|---|
| D1 anchor per state + dim overlay | accepted | structural |
| D2 canvas + TTF + palette + type scale + motion budget | accepted; font family Nunito Bold | structural, font is taste but trivial |
| D3 no context switch: second display or 70/30 split, marimo pre-loaded | accepted | structural (demo) |
| D4 fallback-first phrase, 150 ms crossfade | accepted | structural |
| D5 latch on correct_pose; UI driven by engine state | accepted (also CEO S4) | structural |
| D6 reasoning band bottom third, not right panel | accepted | structural |
| D7 labeling beat at 0:00-0:10 | accepted | demo script |
| D8 `p` key replays a recorded landmark window through the pipeline | accepted (also DX) | structural |
| D9 brand mark (hand glyph, recurring accent) | DEFERRED to Sunday polish | taste |
| D10 TTS | off for the demo unless tested on the demo speaker by 18:00 | consistent with E5 |
| D11 text input: digits only, Enter, Backspace, max 3 chars; test AZERTY on the demo laptop | accepted | structural |
| D12 dashboard chart spec | accepted | structural |
| D13 ending frame with the jury sentence (the one slide) | accepted | demo |
| D14 "left hand" = child's left = screen-left in mirror; one diagram in SPEC | accepted | structural |
| D15 events -> visuals table for all 5 engine events (no arrow for hands_swapped: banner "swap your hands") | accepted | structural |
| TASTE: 35% dim overlay on the video vs keep the video bright | recommend dim (judges read text, not faces); surfaced at gate | taste |

### NOT in scope (design)
Brand mark; TTS voice design; responsive/mobile; screen reader; any web UI.

### What already exists
No UI code yet. MediaPipe landmark drawing utilities for the `d` debug overlay; marimo's built-in plotting for the dashboard.

### Completion summary (design)
Audit: no DESIGN.md, UI scope yes | Step 0: 3/10 | P1 3->8 | P2 2->8 | P3 5->8 | P4 4->7 | P5 0->6 | P6 2->4 | P7 15 resolved, 1 deferred, 1 taste | NOT in scope 5 | Mockups 0 (generation failed, spec is the reference) | Overall 3/10 -> 7/10.

<!-- /autoplan PHASE 3: ENG REVIEW (subagent-only) -->
## 22. Eng Review (autoplan)

### Step 0. Scope challenge
Sub-problems and existing code: see 0B. Minimum set: unchanged from CEO 0D. Complexity check triggers (20 modules) and is accepted: the split follows two process boundaries and one shared file. Search check (in-distribution, no WebSearch): MediaPipe HandLandmarker `num_hands=2` is the built-in [Layer 1]; `weave.Evaluation` with function scorers is the built-in [Layer 1]; `claude -p --allowedTools` + `--mcp-config --strict-mcp-config` is the documented headless path [Layer 1]; there is no built-in "guard" for agent edits, so `guard.py` is legitimate custom code. Completeness check: the plan was happy-path only for the unattended loop; the fixes below make it complete. Distribution: public GitHub repo, `make` targets, no CI (acceptable for a weekend; README replaces CI).

### Eng dual voices
Codex: not installed. CLAUDE SUBAGENT (eng, independent): full output in the conversation; folded below.
```
ENG DUAL VOICES, CONSENSUS TABLE (subagent-only):
  Dimension                        Claude(primary)  Claude(subagent)  Codex  Consensus
  1. Architecture sound?           PARTIAL          PARTIAL           N/A    flagged: commit-before-eval, SPEC leak, normalize contract
  2. Test coverage sufficient?     NO               NO                N/A    flagged: no guard/scorer tests, no mock-claude run
  3. Performance risks addressed?  PARTIAL          PARTIAL           N/A    flagged: tutor blocks frame loop, MCP paging unbounded
  4. Security threats covered?     NO               NO                N/A    flagged: python -c, git *, rules.py reads labels, token in .mcp.json
  5. Error paths handled?          NO               NO                N/A    flagged: no timeouts/retries/revert for 20 unattended iterations
  6. Deployment risk manageable?   PARTIAL          PARTIAL           N/A    flagged: needs caffeinate, second key, 2-iteration rehearsal
```
Every row is flagged from a single voice; all are addressed by G1-G14 below.

### Section 1. Architecture
```
  main repo (../tenfold, humans + orchestrator)          critic worktree (../tenfold-critic, stripped)
  +-- app/  lesson/  data/samples.jsonl (train)          +-- classifier/rules.py   <- the only editable file
  +-- classifier/  eval/  loop/  dashboard/               +-- classifier/features.py, schema.py (read-only)
  +-- SPEC.md (no section 6 levers)  README  Makefile     +-- loop/prompts/*.md, loop/smoke.py, loop/guard.py --check
  |                                                       +-- CLAUDE.md (critic version: rules, success metric, guard checklist)
  |    loop/critic.py (orchestrator, runs here)           |    no SPEC.md, no eval/, no data/, no .env, no git tools
  |      1. clean worktree; copy prompts + last_train_report.json in
  |      2. claude -p diagnostic  (digest in prompt, MCP optional, read-only tools)         @weave.op
  |      3. claude -p patch       (Edit rules.py, Bash(python loop/guard.py --check|smoke.py))  @weave.op
  |      4. claude -p guard agent (diff + diagnosis only, no tools)                         @weave.op
  |      5. guard.py (pinned copy in main repo): only rules.py changed, importable, <80 lines, AST whitelist,
  |         smoke on 6 synthetic windows in a subprocess, tool-input path audit (no '..', no absolute paths
  |         outside the worktree, no 'heldout'/'test.jsonl'), assistant text audit
  |      6. run_eval.py --split train  (Weave project tenfold)      -> metric gate: exact_match not down, no class -5 pts
  |      7. accept: copy rules.py in, git commit as critic-agent, write data/BEST_VERSION; reject: discard, log weave op
  |      8. heldout eval as a subprocess with WANDB_API_KEY_HELDOUT (project tenfold-heldout), never in the critic env
  +-- ../tenfold-heldout/test.jsonl (mode 700, outside both worktrees)
  Live app: loads rules.py at data/BEST_VERSION; classify() wrapped, any exception -> unknown.
  Ablation worktree (../tenfold-blind, branch ablation/blind): same orchestrator, diagnostic step replaced by "improve rules.py"; evaluations suffixed -blind.
```
Findings (all accepted, principle P5 explicit over clever):
- G1 (crit, 9/10) Eval before commit; revert on regression; overnight early-stop disabled, revert kept. Realistic failure: a patch that drops train 10 pts gets built on for 19 iterations. Now impossible.
- G2 (crit, 9/10) SPEC.md is committed and the critic's CLAUDE.md says "read SPEC.md": the critic would read the lever list (section 6), the guard rules and the held-out path. Fix: stripped critic worktree with its own CLAUDE.md; section 6 levers moved out of the committed SPEC into `~/.gstack/projects/codeweave-project/tenfold-levers.md`. USER CHALLENGE at the gate because SPEC section 1 says the file is committed as-is.
- G3 (crit, 8/10) `normalize.py` puts each hand in its own wrist-origin frame, so the inter-hand fingertip distance V0 needs cannot be computed. Fix: `HandFrame` gains `wrist_xy` (mirrored image coords) and `scale`; `features.py` reconstructs a shared frame. This reopens the frozen contract: USER CHALLENGE at the gate.
- G4 (high, 7/10) Two Weave projects from one process and one key: the critic's MCP could list `tenfold-heldout`, and a tool result containing that string would trip the F1 regex. Fix: heldout eval in a subprocess with a second key; audit assistant text and tool inputs only.
- G5 (med, 9/10) molab run button cannot work; local-only, disabled on molab (already E5).
- G6 (med, 7/10) diagnosis via MCP paging is fragile; `eval/last_train_report.json` (per-class accuracy, 10 worst samples as fingertip distances, no raw arrays) in the prompt; MCP optional (E7).
Rollback: `git checkout <sha> -- classifier/rules.py` + `BEST_VERSION`, under a minute.

### Section 2. Code quality
- G7 (high, 9/10) "imports" is not "usable": guard runs rules on 6 synthetic windows (both None, one hand, 3 frames, 5 valid, degenerate, NaN) in a 5 s subprocess and validates domains (method in set, ints 6..10 or None, confidence in [0,1]); scorers treat `output=None` as failure and never raise.
- G8 (med, 8/10) window shorter than 5 frames: contract says 1..5 allowed, engine emits nothing below 5.
- G9 (high, 7/10) ordered left/right labels: capture.py and the live UI instruct "LEFT hand 8, RIGHT hand 7" (mirror view) so ordered labels are correct by construction; keep the user's ordered `exact_match` as primary and add `exact_match_unordered` as a diagnostic. 25 classes stay.
- G10 (med, 6/10) handedness hysteresis: swap the by-x assignment only if the x delta exceeds 0.1 for 3 frames.
- DRY: `classifier/features.py` shared by rules.py, capture.py, kNN (CEO S5). The two undefined scorers get definitions now: `negative_rejection_accuracy` = share of transition/partial_hand/out_of_frame samples returned as unknown; `near_contact_accuracy` = share of near_contact samples with contact=false and method != unknown.
- Diagram maintenance: the architecture diagram above goes into `loop/critic.py` as a module docstring.

### Section 3. Test review
Framework: pytest (Python 3.11). Nothing exists yet, so every path below is a GAP to be written alongside the code.
```
  CODE PATHS                                                        USER / OPERATOR FLOWS
  [+] app/normalize.py                                              [+] Live demo
    +-- [GAP] recenter, scale, wrist_xy/scale carried                +-- [GAP] [->E2E] 8x7 replay window -> correct_pose -> answer
    +-- [GAP] window reset on 0.3 jump; <5 frames -> None            +-- [GAP] one hand -> partial state, no wrong_pose
  [+] classifier/rules.py (V0)                                       +-- [GAP] hand lost 600 ms -> rects return, state latched
    +-- [GAP] 5 hand-built windows per class -> expected state       +-- [GAP] tutor timeout -> fallback phrase, video keeps moving
    +-- [GAP] None/one-hand/NaN -> unknown, never raises             +-- [GAP] Enter on empty -> ignored; wrong answer -> shake
  [+] lesson/engine.py                                              [+] Nightly loop (2 a.m. test)
    +-- [GAP] table-driven: each GestureState -> exactly one event   +-- [GAP] [->E2E] critic.py --iterations 2 --mock-claude: 2 commits,
    +-- [GAP] 300 ms debounce; latch after correct_pose                   2 evaluations, BEST_VERSION updated, no network but W&B
  [+] eval/scorers.py (6)                                            +-- [GAP] mock patch editing eval/scorers.py -> GUARD_REJECT
    +-- [GAP] 10-row fixture with known scores; output=None = fail   +-- [GAP] mock patch that regresses train -> reverted, weave op logged
    +-- [GAP] per_class returns every class incl. unseen             +-- [GAP] kill claude -p mid-run -> iteration failed, loop continues
  [+] loop/guard.py                                                  +-- [GAP] W&B 503 x3 -> backoff, no crash
    +-- [GAP] diff outside rules.py / >80 lines / untracked file /   +-- [GAP] 2-iteration rehearsal with lid closed under caffeinate before 21:00
        schema touched / forbidden import (AST) / smoke fail /      [+] Judge clone (DX)
        domain fail / path audit hit -> GUARD_REJECT line each       +-- [GAP] make doctor on a clean machine; make demo with no keys, no camera
  [+] loop/critic.py                                                 +-- [GAP] run_eval --local prints a table with no W&B key
    +-- [GAP] stop rule on a fake metric sequence
    +-- [GAP] determinism: same rules sha -> same scores
    +-- [GAP] claude timeout 10 min, retry cap 2, then skip
  [+] data/capture.py
    +-- [GAP] F4 trim on a synthetic timeline; stride-5 windows; hand-side instruction in labels
  LLM integration: [->EVAL] the three critic prompts are the product; the Weave evaluations ARE the eval suite.
  COVERAGE: 0/28 paths tested (0%) before implementation; target 28/28 written with the code.
```
The 2 a.m. Friday test is the `--mock-claude` end-to-end run. Hostile QA test: a mock patch that adds a 5-line label lookup (`open('data/samples.jsonl')`) must be rejected by the AST whitelist. Chaos test: SIGKILL on claude -p. Flakiness: nothing in `make test` touches the webcam, Claude, or W&B (use `--local`).
Test plan artifact: `~/.gstack/projects/codeweave-project/ilansainte-agathe-nobranch-eng-review-test-plan-TIMESTAMP.md` (written below).

### Section 4. Performance
- G11 (med, 8/10) the 1.5 s tutor call inside the OpenCV loop freezes video: run it in a thread, show the fallback phrase immediately (Design D4).
- Weave call volume per iteration bounded by the digest; eval must finish under 2 min per split or overnight iterations shrink; log per-sample only in the train evaluation, aggregate for held-out.
- Live: classifier under 3 ms, MediaPipe 20-30 fps, no other hot path.

### Security (folded from the subagent, all accepted)
- G12 (crit, 9/10) `Bash(python -c *)` is arbitrary code: replaced by `Bash(python loop/guard.py --check)` and `Bash(python loop/smoke.py)`; held-out data mode 700 outside both worktrees; sandbox (`sandbox-exec` profile mounting only the worktree) if it fits before 21:00 (TASTE at gate).
- G13 (high, 8/10) rules.py runs inside the eval process and could read labels or patch scorers: AST import whitelist (math, statistics, dataclasses, typing, numpy), forbid open/exec/eval/__import__/getattr/globals/sys/inspect; predictions in a subprocess with cwd outside the repo.
- G14 (high, 8/10) `claude mcp add` at project scope writes the bearer token into a committed `.mcp.json`: use `-s user` or `${WANDB_API_KEY}` expansion; gitignore `*.log`; molab reads keys from molab secrets only; Weave critic ops log prompt + output summary, never env.
- Headless MCP trust: use `--mcp-config loop/mcp.json --strict-mcp-config`; project-scope servers need an interactive approval a fresh worktree never gets.

### Error paths (unattended loop)
`claude -p` subprocess timeout 10 min + `--max-turns 30`; empty diff or GUARD_REJECT retried at most 2x then iteration marked failed; W&B outage in run_eval retried 3x with backoff then sleep 5 min and retry; `nightly.sh` without `set -e`; `weave.finish()` before subprocess exit; humans do not commit to main while the loop holds the worktree (Sunday 9:00 checklist item); ablation evaluations suffixed `-blind`.

### NOT in scope (eng)
CI pipeline (README + make replaces it); Docker packaging; multi-user W&B team setup beyond the second key; learned classifier in the loop (kNN is a reference line only).

### What already exists
See CEO 0B. Nothing rebuilt unnecessarily.

### Failure modes (eng)
```
  CODEPATH         FAILURE                         TEST  HANDLING           SILENT?   CRITICAL GAP?
  critic loop      regression committed             Y     G1 revert          no        no (was yes)
  critic loop      reads held-out / levers          Y     G2 G4 G12          no        no (was yes)
  rules.py         imports but returns garbage      Y     G7 smoke           no        no (was yes)
  normalize        no shared frame for contact      Y     G3                 no        no (was yes)
  nightly          laptop sleeps                    dry   caffeinate+beat    no        no (was yes)
  tutor            blocks frame loop                Y     G11 thread         no        no
  .mcp.json        token committed                  --    G14                no        no (was yes)
```
Critical gaps remaining: 0 (6 closed).

### Worktree parallelization
| Step | Modules | Depends on |
|---|---|---|
| A. contract + capture (landmarks, normalize with G3, schema, capture) | app/, classifier/schema, data/ | — |
| B. rules V0 + features + scorers + run_eval (+ --local) | classifier/, eval/ | A |
| C. critic + guard + prompts + nightly + mock-claude | loop/ | B (interface only) |
| D. engine + tutor + ui + replay | lesson/, app/ui | A (schema) |
| E. dashboard | dashboard/ | B (metrics snapshot) |
Lanes: A first (both people, 30 min). Then Lane 1: B -> C (coequipier). Lane 2: D -> E (Axel). No shared modules across lanes after A. Conflict flag: `classifier/features.py` is written in B and read in D's replay; land B first.

### Completion summary (eng)
Step 0: scope accepted, complexity smell accepted with reason | Architecture 6 issues | Code quality 5 issues | Tests: diagram, 28 gaps (all to be written with the code) | Performance 2 issues | Security 3 high/critical, all mitigated | NOT in scope written | What exists written | TODOS 5 proposed | Failure modes 0 critical gaps after fixes | Outside voice: subagent-only | Parallelization: A then 2 lanes | Lake score 14/14 complete options.

<!-- /autoplan PHASE 3.5: DX REVIEW (DX POLISH, subagent-only) -->
## 23. DX Review (autoplan)

### Step 0. DX investigation
Product type: CLI/local app + a Claude Code agent consumer. Mode: DX POLISH.
```
TARGET DEVELOPER PERSONA
Who:       A hackathon judge (or the W&B sponsor engineer) cloning the public repo Sunday 13:30-16:00 on their own laptop.
Context:   Has 5 minutes between projects, may have no webcam free (Zoom), Python 3.12+ installed, no W&B key handy.
Tolerance: 5 minutes, 3 commands. Abandons on the first traceback.
Expects:   git clone, one install command, one run command, something on screen. A second persona is the critic agent (below).
```
Empathy narrative: I clone the repo. README says three commands. `pip install -r requirements.txt` fails on Python 3.13 because there is no mediapipe wheel; I have no idea why. I switch to 3.11 and it installs. `python app/main.py` crashes at `weave.init` asking for a W&B login; I do not have a key on this laptop. I add a dummy key, the webcam is held by Zoom, black frame, no message. I give up and watch the video instead. If instead `make doctor` had told me "Python 3.13: use 3.11 (`.python-version`)", "no WANDB_API_KEY: tracing off", "camera busy: run `make demo`", I would have seen 8 x 7 recognized from the recorded replay in under three minutes.
Competitive benchmark (no WebSearch; reference tiers): Stripe 30 s, Vercel 2 min, Docker hello-world 5 min. Current estimated TTHW: 20-30 min (Python mismatch, W&B account, gesture learning); first critic iteration 40+ min. Target: under 5 min to see 8 x 7 recognized via replay with no keys; under 10 min to run one guarded critic iteration with keys.
Magical moment: `make demo` plays a recorded landmark window through the real pipeline and the screen goes green with "56" in 200 px, no webcam, no keys (delivery vehicle B, copy-paste demo command).
Journey trace and confusion report: folded into the findings table.

### DX dual voices
Codex: not installed. CLAUDE SUBAGENT (DX, independent): full output in the conversation.
```
DX DUAL VOICES, CONSENSUS TABLE (subagent-only):
  Dimension                        Claude(primary)  Claude(subagent)  Codex  Consensus
  1. Getting started < 5 min?      NO               NO                N/A    flagged (X1-X4)
  2. API/CLI naming guessable?      PARTIAL          PARTIAL           N/A    flagged (no flags defined)
  3. Error messages actionable?     NO               NO                N/A    flagged (X7, X10)
  4. Docs findable & complete?      PARTIAL          PARTIAL           N/A    flagged (X11)
  5. Upgrade path safe?             YES              YES               N/A    agree (frozen contracts + one mutable file)
  6. Dev environment friction-free? NO               NO                N/A    flagged (X10, X12)
```

### Findings (all auto-decided, P5 fewer steps, P1 problem+cause+fix)
| # | Finding | Decision |
|---|---|---|
| X1 (crit) | `weave.init` unconditional in app and eval | accepted: init only if `WANDB_API_KEY` set, else one line "tracing off"; `run_eval.py --local` prints a table |
| X2 (high) | undeclared prerequisites | accepted: `.python-version` = 3.11, README Prerequisites block (macOS camera permission, MediaPipe model download), `.env.example` annotated required-for-live / loop / optional |
| X3 (crit) | no camera-less path | accepted: `camera.py --source video.mp4`; `make demo` replays a recorded landmark window (Design D8); README states the loop needs no camera |
| X4 (med) | judge does not know the gesture | accepted: `make demo` and the `p` key show a correct 8 x 7 before the judge tries |
| X5 (high) | critic does not know what success is | accepted: `patch.md` states "success = train exact_match up while false_unknown_rate < 5%; other scorers are diagnostics" |
| X6 (high) | critic learns guard rules by being rejected | accepted: guard checklist in `patch.md` and the critic CLAUDE.md; `python loop/guard.py --check` allowed before finishing |
| X7 (high) | guard rejection text undefined | accepted: `GUARD_REJECT <rule>: <what> \| cause: <why> \| fix: <change>` one line per failed check, fed back verbatim with the diff |
| X8 (med) | F1 substring scan false positives | accepted: scan tool inputs and assistant text only (Eng G4); forbidden strings listed in the reject message |
| X9 (med) | MCP data shape unspecified | accepted: `eval/last_train_report.json` is the primary source (Eng G6) |
| X10 (high) | no preflight | accepted: `make doctor` checks python version, mediapipe import, camera, keys, `claude` on PATH, MCP registered; PASS/FAIL per line with the fix |
| X11 (med) | README has headings, no commands | accepted: the three real commands, a "run one iteration by hand" block, a "what the critic sees / may touch / guard rejects" box |
| X12 (med) | no task runner | accepted: Makefile with run, demo, eval, eval-local, loop N=1, doctor, test |
| X13 | escape hatches | accepted: `--source`, `--local`, `critic.py --iterations 1 --dry-run`; DEFERRED to TODOS: offline replay of a stored patch through the guard |

### Developer journey map
```
STAGE          DEVELOPER DOES                        FRICTION (before)                  STATUS
1 Discover     reads README jury sentence             fine                               ok
2 Evaluate     scans architecture + Weave links       links may need login               ok (public project)
3 Install      3 commands                             python version, wheel               fixed (X2)
4 Hello world  make demo                              needed camera + keys                fixed (X1, X3, X4)
5 Integrate    make run with webcam                   camera permission message           fixed (X10)
6 Debug        make doctor                            did not exist                       fixed (X10)
7 Loop         make loop N=1 with keys                MCP registration, guard rules       fixed (X6, X7, X11)
8 Upgrade      git pull, BEST_VERSION pointer         none                               ok
9 Scale        n/a this weekend                       —                                  n/a
```

### DX scorecard
```
  Dimension          Score (before -> after)
  Getting Started    2 -> 8
  API/CLI/SDK        4 -> 7
  Error Messages     1 -> 7
  Documentation      3 -> 7
  Upgrade Path       8 -> 8
  Dev Environment    2 -> 7
  Community          5 -> 5   (public repo, no channel; acceptable)
  DX Measurement     1 -> 3   (not a goal this weekend)
  TTHW               25 min -> < 5 min (replay), critic iteration 40 -> < 10 min
  Competitive rank   Needs Work -> Competitive
  Magical moment     designed via `make demo` (copy-paste command)
  Mode               DX POLISH
  Overall DX         3 -> 7
  Principle coverage: Zero friction covered | Learn by doing covered (demo, dry-run) | Fight uncertainty covered (doctor, GUARD_REJECT) | Opinionated + escape hatches covered | Code in context partial (README blocks) | Magical moment covered
```

### DX implementation checklist
[ ] TTHW < 5 min via `make demo` with no keys, no camera  [ ] `.python-version` 3.11  [ ] `make doctor` PASS/FAIL lines with fixes  [ ] every error message = problem + cause + fix  [ ] `GUARD_REJECT` structured lines  [ ] `patch.md` states the success metric and the guard checklist  [ ] `.env.example` annotated  [ ] README: 3 real commands + manual iteration block + critic box  [ ] Makefile targets  [ ] `run_eval.py --local`  [ ] `critic.py --dry-run`

### NOT in scope (DX)
Package publishing, docs site, community channel, DX telemetry.

### What already exists
Nothing yet; `claude mcp` and `weave` CLIs provide their own help text.

### Completion summary (DX)
Persona: judge + critic agent | TTHW 25 -> <5 min | 13 findings, 12 accepted, 1 deferred | Scorecard overall 3 -> 7 | Outside voice: subagent-only.

## 24. Cross-phase themes
- **The critic can defeat its own guard** (CEO P6/E8, Eng G1/G2/G12/G13, DX X6/X8): three independent reviewers. High-confidence. Fix set: stripped worktree, no git/python -c tools, AST whitelist, eval before commit, second W&B key.
- **Ungated commits make the curve fall** (CEO E2, Eng G1). High-confidence.
- **The nightly can die silently** (CEO E10, Eng 2 a.m. list). High-confidence.
- **The demo must not depend on live recognition or LLM latency** (CEO E10, Design D3/D4/D8, DX X3/X4). High-confidence: replay key, fallback-first phrase, no alt-tab.
- **MCP-only diagnosis is fragile** (CEO E7, Eng G6, DX X9). High-confidence: precomputed failure report, MCP optional.
- **The molab button cannot work** (CEO E5, Eng G5, Design decision 10). Feasibility, not preference.
- **The committed SPEC contradicts itself and leaks to the critic** (CEO E11, Eng G2). High-confidence.

## 25. Decision Audit Trail
<!-- AUTONOMOUS DECISION LOG -->
| # | Phase | Decision | Classification | Principle | Rationale | Rejected |
|---|---|---|---|---|---|---|
| 1 | CEO | Mode SELECTIVE EXPANSION | mechanical | autoplan | forced | other modes |
| 2 | CEO | Approach B over A/C | mechanical | P1 | complete proof, keeps demo | A (pivot only), C (conditional) |
| 3 | CEO | E1 kNN line accepted | mechanical | P1 | 20 min, answers "why not ML" | skip |
| 4 | CEO | E2 metric gate accepted | mechanical | P1 | curve can fall without it | none |
| 5 | CEO | E3 relabel key conditional | TASTE | P6 | strongest story vs live risk | include now / cut |
| 6 | CEO | E4 3-5 strangers held-out | mechanical | P1 | one person too little | one person |
| 7 | CEO | E5 scope cuts | USER CHALLENGE | P3 | reorders user's spec | — |
| 8 | CEO | E6 detection-ceiling metrics | mechanical | P1 | premise 3 unverified | none |
| 9 | CEO | E7 Weave API digest | mechanical | P3 | MCP truncates | MCP-only |
| 10 | CEO | E8 critic hardening | mechanical | P1 | blindness must be enforced | grep-only |
| 11 | CEO | E9 hold-level scoring + CI | mechanical | P1 | honest error bars | window-level |
| 12 | CEO | E10 nightly ops | mechanical | P6 | silent death regret | none |
| 13 | CEO | E11 fold fixes into SPEC | mechanical | P5 | two instances build to text | keep appendix |
| 14 | CEO | E12 W&B Inference critic arm | deferred | P3 | time | — |
| 15 | CEO | E13 browser app | deferred | P3 | scope | — |
| 16 | CEO | E14 ablation as headline | mechanical | P6 | rarest claim | curve-only |
| 17 | CEO | E15 keep guard agent + add gate | user direction | — | F2 specified by user | drop agent |
| 18 | CEO | catch-all except only around classify() | mechanical | P5 | demo must never crash | none |
| 19 | Design | skip mockup board, one reference image | mechanical | P3 | board needs interactive feedback | full board |
| 20 | Design | D1 anchor per state + dim overlay | structural / TASTE on overlay | P5 | judges read text not faces | bright video |
| 21 | Design | D2 canvas, TTF Nunito, palette, type scale, motion budget | structural | P5 | Hershey fonts read as CS project | keep cv2 text |
| 22 | Design | D3 no alt-tab; second display or split | structural | P5 | cliff at 1:30 | alt-tab |
| 23 | Design | D4 fallback-first phrase | structural | P1 | hides 1.5 s latency | wait |
| 24 | Design | D5 latch + engine-state UI | structural | P5 | flicker | raw gesture |
| 25 | Design | D6 reasoning band bottom third | structural | P5 | right panel overlaps right hand | right panel |
| 26 | Design | D7 labeling beat 0:00-0:10 | demo | P1 | judges don't know the trick | start on 8x7 |
| 27 | Design | D8 `p` replay key | structural | P1 | demo safety | none |
| 28 | Design | D9 brand mark | deferred | P3 | Sunday polish | now |
| 29 | Design | D10 TTS off unless tested by 18:00 | mechanical | P6 | robotic voice kills premium | on |
| 30 | Design | D11-D15 input, chart spec, ending frame, mirror diagram, events table | structural | P5 | remove ambiguity | — |
| 31 | Eng | G1 eval before commit + revert | mechanical | P5 | regressions compound | commit first |
| 32 | Eng | G2 stripped critic worktree; levers out of committed SPEC | USER CHALLENGE | P5 | SPEC leaks to critic | — |
| 33 | Eng | G3 HandFrame wrist_xy + scale | USER CHALLENGE (frozen contract) | P1 | V0 cannot compute contact otherwise | — |
| 34 | Eng | G4 second W&B key, audit inputs not results | mechanical | P1 | false positives + leakage | one key |
| 35 | Eng | G7 smoke + domain validation | mechanical | P1 | import != usable | import-only |
| 36 | Eng | G9 correct-by-construction hand assignment, keep ordered labels | mechanical | P3 | respects user's ordered design | unordered 15 classes |
| 37 | Eng | G12 replace python -c with two allowed scripts | mechanical | P1 | arbitrary code | keep |
| 38 | Eng | sandbox-exec profile for the critic | TASTE | P1 vs time | rigor vs 30-60 min | whitelist only |
| 39 | Eng | G13 AST whitelist + subprocess predictions | mechanical | P1 | label lookup cheat | none |
| 40 | Eng | G14 MCP user scope, gitignore logs | mechanical | P1 | token in public repo | project scope |
| 41 | Eng | all 28 test gaps written with the code | mechanical | P1 | completeness cheap with CC | defer |
| 42 | Eng | scorer definitions written now | mechanical | P5 | frozen at 13:00 | later |
| 43 | DX | X1-X12 accepted | mechanical | P5, P1 | fewer steps, actionable errors | — |
| 44 | DX | X13 offline patch replay | deferred | P3 | time | now |

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR (PLAN via /autoplan) | 15 proposals, 12 accepted, 2 deferred, 1 user challenge accepted |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | Codex not installed; Claude subagents used in all 4 phases |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR (PLAN via /autoplan) | 14 issues, 0 critical gaps after fixes, 28 test gaps to write with the code |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | issues_open (PLAN via /autoplan) | score 3/10 -> 7/10, 15 decisions; brand mark deferred |
| DX Review | `/plan-devex-review` | Developer experience gaps | 1 | issues_open (PLAN via /autoplan) | score 3/10 -> 7/10, TTHW 25 min -> under 5 min |

- **VERDICT:** CEO + ENG CLEARED, ready to implement. Design and DX scores are 7/10 with the remaining points deferred on purpose (brand mark, TTS, community). Status: APPROVED at the /autoplan gate on 2026-09-12 13:12 PT; user accepted all three challenges (scope cuts, stripped critic worktree with levers out of the committed SPEC, HandFrame wrist_xy + scale) and the three taste picks.
NO UNRESOLVED DECISIONS
